'use strict';

const tree =  require('../tree/tree.js');

describe('tree', () => {
  describe('findMaxValue', () => {
    it('will return the largeest value that is found in the tree', () => {
      let 
    })
  })
})